<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Scribex – Edit Note</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gray-900 text-white min-h-screen p-4 md:p-6 lg:p-10">

  <div class="bg-gray-800 p-4 shadow-lg mb-6">
    <h1 class="text-2xl font-bold pl-4">Scribex</h1>
  </div>

  <div class="max-w-3xl mx-auto bg-zinc-800 p-6 md:p-10 rounded-xl shadow-xl">

    <h1 class="text-3xl font-bold mb-6">Edit Note</h1>

    <form action="/update/<%= data.fileName %>" method="post" class="space-y-6">

      <input type="hidden" name="oldFileName" value="<%= data.fileName %>">

      <div>
        <label class="block mb-2 text-gray-300">Title</label>
        <input 
          type="text"
          name="title"
          value="<%= data.title %>"
          class="w-full p-3 rounded bg-zinc-700 outline-none"
          required
        />
      </div>

      <div>
        <label class="block mb-2 text-gray-300">Description</label>
        <textarea 
          name="description"
          rows="10"
          class="w-full p-3 rounded bg-zinc-700 outline-none"
          required
        ><%= data.description %></textarea>
      </div>

      <div class="flex flex-wrap gap-4">
        <a href="/first/<%= data.fileName %>" class="bg-blue-500 px-4 py-2 rounded font-semibold hover:bg-blue-600">Back</a>
        <button type="submit" class="bg-green-500 px-6 py-2 rounded font-semibold hover:bg-green-600">Save Changes</button>
      </div>

    </form>

  </div>

</body>
</html>
